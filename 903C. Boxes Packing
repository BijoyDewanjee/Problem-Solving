#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    while(cin>>n){
        map<int,int>mp;
        int x,mx=0;
        for(int i=1;i<=n;i++){
            cin>>x;
            mp[x]++;
            mx=max(mx,mp[x]);
        }
        cout<<mx<<endl;
    }

}
