# include<bits/stdc++.h>
using namespace std;
 
int main()
{
    string s,s1;
    while(cin>>s>>s1){
        if(s==s1) cout<<"-1"<<endl;
        else {
           int maximum= max(s.size(),s1.size());
            cout<<maximum<<endl;
        }
    }
    return 0;
}
