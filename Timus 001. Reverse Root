
#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main()
{
   ll n;
     vector<ll>v;
    while(scanf("%lld", &n) != EOF){

        v.push_back(n);

    }


     for(int i=v.size()-1;i>=0;i--){
        printf("%0.4lf\n",sqrt(v[i]));
     }
}
