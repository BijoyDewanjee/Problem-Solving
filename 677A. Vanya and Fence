# include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,h,a[1000],s,sum=0,t;
 
   while(cin>>n>>h){
    for(int i=0;i<n;i++){
        cin>>a[i];
        if(a[i]<=h) s=1;
        else s=2;
        sum+=s;
    }
    cout<<sum<<endl;
     sum=0;
    }
 
    return 0;
}
